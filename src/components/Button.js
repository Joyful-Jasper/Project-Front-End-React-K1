import React from "react";

const Button = () => {
  return <button style={{ backgroundColor: "#ffffff", borderColor: "#ffffff", color: "#FFA000", borderRadius: 30, paddingRight: 20, paddingLeft: 20 }}>Button</button>;
};

export default Button;
